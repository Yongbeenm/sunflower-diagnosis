# components/

`ui/`     shadcn/ui primitives. Owned source, edit freely.
`layout/` app shell, header, nav, language and theme toggles, footer.

Anything feature-specific belongs in that feature slice, not here.
